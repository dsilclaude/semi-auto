# projects/

실제 측정/자동화 프로젝트를 보관하는 곳입니다.
새 프로젝트는 이 아래에 `projects/<프로젝트명>/` 형태로 추가하세요.

```
projects/
├── old/            # 이관 이전의 기존 측정 노트북 (보관용)
└── <프로젝트명>/    # 앞으로 추가할 실제 프로젝트
```

> 단순 예제나 기능 검증용 코드는 여기가 아니라 `examples/<장비명>/` 아래에 둡니다.
